version: '3'

services:
  bitpreco-arbitrage:
    image: bitpreco-arbitrage
    container_name: bitpreco-arbitrage
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /
    volumes:
          - .:/home/node/app/
    env_file: .env
    restart: unless-stopped